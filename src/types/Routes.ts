export enum pageRoutes {
  create = 'create',
  wishlist = 'wishlist',
  cart = 'cart',
  event = 'event/:title/:id',
  index = '/',
}

export enum pageLinks {
  home = '/',
  create = '/create',
  wishlist = '/wishlist',
  event = '/event',
  cart = '/cart',
}
